# Preprocessing pipeline dependencies (run_preprocess.py + 1-…→4-…).
# Authoritative set — mirrors build_launcher.py:DEPS ("numpy Pillow h5py scipy tqdm").
h5py>=3.8.0
numpy>=1.24.0
scipy>=1.10.0
Pillow>=10.0.0
tqdm>=4.65.0

# Optional — only needed for the download/ bundle (--with-downloads): OME-TIFF export.
# The .bat launcher installs it on demand; uncomment for a manual `pip install -r`.
# tifffile>=2024.1.0
